{
  "name": "@modelcontextprotocol/server-fake-qdrant",
  "version": "0.1.0",
  "description": "Local fake Qdrant implementation exposed via MCP tools and an optional HTTP shim.",
  "license": "MIT",
  "type": "module",
  "mcpName": "io.github.modelcontextprotocol/server-fake-qdrant",
  "bin": {
    "mcp-server-fake-qdrant": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.24.3",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.1",
    "@vitest/coverage-v8": "^2.1.8",
    "shx": "^0.3.4",
    "typescript": "^5.6.2",
    "vitest": "^2.1.8"
  }
}

