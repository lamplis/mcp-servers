# URLs to Index
# Add one URL per line. Lines starting with # are ignored.
# Each URL will be crawled recursively (same-domain links, up to 100 pages).
# URLs are re-crawled after 30 days (configurable via DOCSEARCH_CRAWL_LIFETIME_DAYS).

# Example:
# https://docs.example.com/getting-started
# https://www.typescriptlang.org/docs/handbook/

https://context7.com/websites/typescriptlang/llms.txt?tokens=10000
